import axios from "axios";
import { useSelector } from "react-redux";

export default function ReceiptItem(recepitArry, remove){
    const token = useSelector(s => s.user.token);

    return(
        <></>

    );
}